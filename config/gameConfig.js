const gameConfig = {
  NUM_CORRUPTION_TYPES: 10, // Número de tipos de corrupción para la ruleta
  NUM_CORRUPTION_CARDS: 5,  // Número de sub-opciones (cartas) por tipo de corrupción
};

module.exports = gameConfig;
